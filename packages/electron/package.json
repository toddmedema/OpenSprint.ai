{
  "name": "@opensprint/electron",
  "productName": "Open Sprint",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop packaging workspace for OpenSprint",
  "author": "OpenSprint Team",
  "main": "dist/main.js",
  "scripts": {
    "compile": "tsc",
    "start": "npm run compile && electron .",
    "build": "npm run compile && electron-builder",
    "build:dir": "npm run compile && electron-builder --dir",
    "build:mac": "npm run compile && electron-builder --mac",
    "build:win": "npm run compile && electron-builder --win",
    "build:linux": "npm run compile && electron-builder --linux"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^35.1.0",
    "electron-builder": "^25.0.0",
    "sharp": "^0.33.5",
    "typescript": "^5.7.2"
  },
  "build": {
    "appId": "com.opensprint.app",
    "productName": "Open Sprint",
    "electronVersion": "35.1.0",
    "directories": {
      "output": "dist"
    },
    "files": [
      "dist/main.js",
      "dist/preload.js"
    ],
    "extraResources": [
      {
        "from": "desktop-resources/backend",
        "to": "backend"
      },
      {
        "from": "desktop-resources/frontend",
        "to": "frontend"
      }
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "dmg"
      ],
      "icon": "../frontend/public/desktop-icon-mac.png"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "../frontend/public/logo-512x512.png"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "icon": "../frontend/public/logo-512x512.png"
    }
  }
}
